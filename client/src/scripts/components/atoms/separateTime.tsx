// react
import React from 'react'
// styles
import styles from '@/styles/components/atoms/separateTime.module.sass'

type Props = {
}

export default (props: Props): JSX.Element  => {
    
   return (
        <div className={styles['item']}>：</div>
    )
}